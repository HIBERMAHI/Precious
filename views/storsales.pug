doctype html
html
  head
    meta(charset='UTF-8')
    title Sales Page - Nyondo Hardware
    link(rel='stylesheet' href='/css/storedash.css')
    link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css')

  body
    .sidebar
      .logo
        h3 ⚒ NYONDO HARDWARE

      ul
        li
          a(href='storedash')
            i.fas.fa-home
            |  Dashboard

        li
          a(href='invento')
            i.fas.fa-boxes
            |  Inventory

        li.active
          a(href='storsales')
            i.fas.fa-cash-register
            |  Sales

        li
          a(href='addstock')
            i.fas.fa-plus-circle
            |  Add Stock

        li
          a(href='logout')
            i.fas.fa-sign-out-alt
            |  Reports

    .main
      .header
        h1
          i.fas.fa-chart-bar
          |  Sales Overview

      // CARDS
      .cards

        .card
          .card-content
            i.fas.fa-coins

            div
              h3 Sales Revenue
              p #{stats.salesRevenue.toLocaleString()} UGX

        .card
          .card-content
            i.fas.fa-shopping-cart

            div
              h3 Items Sold
              p #{stats.itemsSold.toLocaleString()}

        .card
          .card-content
            i.fas.fa-chart-line

            div
              h3 Profit
              p UGX 350,000

      // TABLE
      .table-section

        .table-header
          h2
            i.fas.fa-receipt
            |  Recent Sales

          input(type='text' placeholder='Search sales...')

        table
          thead
            tr
              th Date
              th Product Name
              th Quantity
              th Price (UGX)
              th Total (UGX)
              th Attendant

              if user.role === 'salesattendant' || user.role === 'admin'
                th Actions

          tbody
            each sale in dbSales
              tr
                td #{new Date(sale.date).toLocaleString()}

                td
                  #{sale.productName ? sale.productName.productName : 'Item deleted'}

                td #{sale.quantity}

                td #{sale.price}

                td #{sale.totalAmount}
                td #{sale.attendant ? sale.attendant.fullname : 'unknown'}

                if user.role === 'salesattendant' || user.role === 'admin'
                  td